presets/agents/atoms

timeline bar
component input bar
layers bar
drag & dropper bar

first build for output.
next make each preset run and only edit what it needs to edit in the final output, not rewrite everything from the start.

merge presets & agents (into 1 in UI)

what are presets primarily

- create typography for script.
- animation style for clips.
- animation style for text.
- i select x no. of caption nodes, and applied one preset to it ( it should be appliable, for it to be appliable, in preset metadata you should explain that this preset works like that otherwise editor will not know.)
- transition effects between 2 images
- transition effects between 4 images etc...
- agents shoudl be accessible in the editor otherwise waste
- timeline bar is for time adjustment of final output, messing it should mess something there as well as in the base data captions accordingly.
- in a simialr way, if a video timing is messed up in editor, it should store that related offsets in the metadata MAYBE to make the preset work without involving the timeshifts.

2 types of presets.

1. presets that create the nodes.
2. presets that replace the ndoes.
